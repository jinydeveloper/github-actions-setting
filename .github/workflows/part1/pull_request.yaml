# 
name: pull-request-workflow
on: 
  pull_request:
    # 이벤트가 특정 타입에서만 발생하도록 세부 설정 추가
    types: [opened]


jobs:
    pull-request-job:
        runs-on: ubuntu-latest
        steps:
        # name은 생략해도 됨
        - name: step1
          run: echo Hello, World!
        - name: step2
        # | 사용 시 멀티라인 문자열을 사용할 수 있음
          run: |
            echo hello world
            echo github actions